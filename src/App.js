import './App.css';
import Router from '';
import ThemeProvider from '';

import React from 'react';

const App = () => {
  return (
    <ThemeProvider>
      <Router />
    </ThemeProvider>
  );
};

export default App;
